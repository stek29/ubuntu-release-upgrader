Images created with:

$ bzr get http://bazaar.launchpad.net/~mvo/ubuntu-jeos/mvo
$ ./ubuntu-jeos-builder --vm kvm --kernel-flavor generic --suite feisty --ssh-key `pwd`/ssh-key.pub  --components main,restricted  --rootsize 20G --no-opt

make sure you copy the ssh-key into the profile dir so that the upgrade
tester can access it
